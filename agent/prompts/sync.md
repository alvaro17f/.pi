---
description: Generate conventional commit, commit staged changes and push
---
Generate a conventional commit message for the staged changes using `git diff --cached`. Then commit with that message. Push the changes to the remote repository.
