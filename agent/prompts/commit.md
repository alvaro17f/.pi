---
description: Generate conventional commit and commit staged changes (no push)
---
Generate a conventional commit message for the staged changes using `git diff --cached`. Then commit with that message. Do NOT push.